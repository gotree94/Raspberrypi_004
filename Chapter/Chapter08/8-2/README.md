# 8-2 라이브러리 설치 및 확인

* 자율주행 모델을 실행하기 위한 필수 라이브러리 설치하고 동작 여부를 확인합니다.
*  PyTorch, OpenCV, NumPy등 모델 구동에 필요한 패키지를 설치하고, 버전 호환성 문제를 점검 합니다.
*  환경 구성이 완료되면 간단한 테스트 코드를 실행하여 정상적으로 모델을 불러올 수 있는지 확인합니다.

## 라이브러리 설치하기

```
pip install torch opencv-python numpy
```

## 라이브러리 및 파이썬 버전 확인

* 8_2_1.py

```python


```

```
pip install torch==2.8.0
pip install opencv-python==4.12.0
PiP install numly==!.26.4
```
