# 5. 가상 스크리닝

## 5.1 개요

`src/screening/` 모듈은 유사도 검색, Pharmacophore 매칭, 화합물 라이브러리 관리를 통해 대규모 화합물 라이브러리에서 히트를 발굴합니다.

## 5.2 유사도 검색

```python
from src.screening.similarity_search import SimilaritySearcher

searcher = SimilaritySearcher()
results = searcher.search(
    query_smiles="CC(=O)Oc1ccccc1C(=O)O",
    library=["CCO", "c1ccccc1", "CC(=O)O", ...],
    top_n=10,
    threshold=0.5,
)

for smi, score in results:
    print(f"{smi}: {score:.3f}")
```

## 5.3 Pharmacophore 분석

```python
from src.screening.pharmacophore import PharmacophoreGenerator, PharmacophoreMatcher

generator = PharmacophoreGenerator()
query_pharm = generator.generate_from_smiles("CC(=O)Oc1ccccc1C(=O)O")

matcher = PharmacophoreMatcher()
matches = matcher.match_library(query_pharm, library_smiles)
```

## 5.4 라이브러리 관리

```python
from src.screening.library_manager import CompoundLibraryManager

manager = CompoundLibraryManager()
manager.load_from_sdf("library.sdf")
manager.load_from_smiles_file("compounds.smi")
manager.filter_by_properties(min_mw=200, max_mw=500)
manager.deduplicate()
stats = manager.get_statistics()
```
