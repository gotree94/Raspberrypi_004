# 4. 분자 모델링 파이프라인

## 4.1 개요

`src/molecular/` 모듈은 RDKit을 활용한 분자 처리, 지문 계산, 기술자 추출을 담당합니다.

## 4.2 RDKit 유틸리티

```python
from src.molecular.rdkit_utils import (
    MolFromSMILES,
    calculate_all_descriptors,
    is_drug_like,
)

mol = MolFromSMILES("CC(=O)Oc1ccccc1C(=O)O")
descriptors = calculate_all_descriptors(mol)
# MolWt, MolLogP, NumHDonors, NumHAcceptors, TPSA, etc.

if is_drug_like(mol):
    print("Passes Lipinski filter")
```

## 4.3 분자 지문

7가지 지문 생성기 지원:

```python
from src.molecular.fingerprint import FingerprintGenerator, similarity

gen = FingerprintGenerator()
fp_morgan = gen.morgan_fingerprint(mol, radius=2, nBits=2048)
fp_rdkit = gen.rdkit_fingerprint(mol)
fp_ap = gen.atom_pair_fingerprint(mol)
fp_torsion = gen.torsion_fingerprint(mol)
fp_maccs = gen.maccs_fingerprint(mol)
fp_pharm = gen.pharmacophore_fingerprint(mol)
fp_erg = gen.erg_fingerprint(mol)

# 유사도 계산
sim = similarity(fp_morgan, fp2, metric="tanimoto")
```

## 4.4 기술자 계산

```python
from src.molecular.descriptors import DescriptorCalculator

calc = DescriptorCalculator()
desc = calc.calculate_all(mol)                  # 30+ descriptors
druglike = calc.drug_likeness_profile(mol)       # Lipinski + Veber
physchem = calc.physicochemical_properties(mol)  # MW, LogP, etc.
```
